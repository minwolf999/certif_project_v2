= @form.simple_fields_for :answers do |answer_f|
  div class='flex justify-evenly'
    = answer_f.input :title, label: "Answer #{(answer_f.options[:child_index] || 0) + 1}"
    = answer_f.input :good, as: :boolean
